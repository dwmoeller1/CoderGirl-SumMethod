using System;

namespace SumMethod
{
    public static class Program
    {
        public static void Main()
        {
            Console.ReadLine();
        }

        // TODO: Create a method that returns the sum of all integer numbers up to and including given value. 
        // Use the equation (n * (n + 1)) / 2.
        // For example, if the method gets passed a value of 4, it would return the value of 1 + 2 + 3 + 4 
        // (the equation provided will do this calculation)
        // The method
            // must be named Sum
            // must by public
            // must take a single parameter
    }
}
